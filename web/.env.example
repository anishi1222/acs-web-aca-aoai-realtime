# Copy this file to .env.local (recommended) and fill in values
# cp .env.example .env.local

# Optional: if you want to point the web dev server at a non-default API base.
# With the included Vite proxy, you can usually leave this unset.
# VITE_API_BASE=http://localhost:8000
